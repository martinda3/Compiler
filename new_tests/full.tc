int main() {
  int i;
  int k;
  k = 12;
  /*
  i = 10 + 1;
  write(i);
  if (i < k) {
  write("i is bigger");
  return 0;
  }
  else {
  write("k is bigger");
  return 1;
  }
  i = i * 1;
  write(i);
  ;
  newline; */
  // This is a comment int test;
  write("k is "); // This is after good code;#
  write(k);
  k = 10 / 2;
  write("k is  after 10 / 2");
  write(k);
  k = k / 2;
  write("k is  after k / 2");
  write(k);
}
