/*******************************************
 * Tested:
 * 2 function definitions with simple
 * implementations
 *******************************************/

int main() {
  int i;
  int k;
  k = 12;
  write("k is ");
  write(k);
  k = 10 / 2;
  write("k is after 10 / 2");
  write(k);
  k = k / 2;
  write("k is after k / 2");
  write(k);
}

int test() {
	int g;
	g = 0;
	write(g);
}
